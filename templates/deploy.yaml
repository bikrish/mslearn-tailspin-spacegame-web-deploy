parameters:
  WebAppName: 

steps:
- download: current
  artifact: drop
- task: AzureWebApp@1
  displayName: 'Azure App Service Deploy: website'
  inputs:
    azureSubscription: 'biksvc'
    appName: '${{parameters.WebAppName}}'
    package: '$(Pipeline.Workspace)/drop/$(buildConfiguration)/*.zip'